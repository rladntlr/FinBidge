spring:
  application:
    name: FinBridge

  datasource:
    url: jdbc:mysql://localhost:3307/finbridge?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: all
      retries: 3
    consumer:
      group-id: finbridge-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      auto-offset-reset: earliest

  batch:
    job:
      enabled: false

sftp:
  host: ${SFTP_HOST:localhost}
  port: ${SFTP_PORT:2222}
  username: ${SFTP_USERNAME:finbridge}
  password: ${SFTP_PASSWORD:finbridge123}  # 운영 환경에서는 반드시 환경변수로 주입할 것
  upload-dir: ${SFTP_UPLOAD_DIR:/upload}
  strict-host-key-checking: "yes"
  known-hosts: ${user.home}/.ssh/known_hosts

server:
  port: 8080

logging:
  level:
    com.finbridge: DEBUG
    org.springframework: INFO
    org.apache.kafka: WARN
    org.flywaydb: INFO
